# Example environment variables for development / deployment
# Do NOT commit real secrets to your repo.

# Firebase credentials: either set the JSON string in FIREBASE_KEY (escaped),
# or provide a path to a service-account JSON file using FIREBASE_CREDENTIALS_PATH.
# Example (for local dev): FIREBASE_CREDENTIALS_PATH=firebase_key.json (file in backend/ folder)
# Example (for Render): FIREBASE_CREDENTIALS_PATH=/path/to/firebase-credentials.json (absolute or relative to app root)
FIREBASE_KEY=
FIREBASE_CREDENTIALS_PATH=firebase_key.json

# Paths to model artifacts (relative to `backend/` by default)
MODEL_PATH=eeg_age_classifier.pkl
BASELINES_PATH=age_group_baselines.pkl

# Server settings
HOST=0.0.0.0
PORT=8000

# Other optional settings
# LOG_LEVEL=info

